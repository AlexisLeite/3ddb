# Repository Instructions

- After making any code or configuration change, run `npm run lint-modules`.
- If `npm run lint-modules` fails, split oversized files before considering the change complete.
- `npm run lint-modules` also enforces that each code file contains at most one class or component.
